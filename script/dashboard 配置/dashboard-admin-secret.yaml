# Kubernetes Dashboard 长期有效Token配置
# 创建Secret方式，token不会过期且不会改变

apiVersion: v1
kind: Secret
metadata:
  name: dashboard-admin-token
  namespace: kube-system
  annotations:
    kubernetes.io/service-account.name: dashboard-admin
type: kubernetes.io/service-account-token
